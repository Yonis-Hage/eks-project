grafana:
  adminPassword: "admin"
  service:
    type: LoadBalancer
  ingress:
    enabled: true
    hosts:
      - "grafana.eks.yonishage.co.uk"  

prometheus:
  service:
    type: LoadBalancer

alertmanager:
  service:
    type: LoadBalancer

grafana:
  ingress:
    enabled: true
    hosts:
      - grafana.eks.yonishage.co.uk
    annotations:
      kubernetes.io/ingress.class: nginx
    path: /